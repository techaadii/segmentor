# Segmentor

A human-in-the-loop in-context learning framework for semantic segmentation.

## Installation

### Using `pip`

Install the package from GitHub with `pip`

```bash
pip install "git+https://github.com/sattwik-sahu/segmentor.git"
```
